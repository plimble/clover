pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'docker -v'
            }
        }
        stage('Test'){
            steps {
                sh 'docker -v'
            }
        }
        stage('Deploy') {
            steps {
                sh 'docker -v'
            }
        }
    }
}
